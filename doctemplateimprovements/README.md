# Template Improvements — Merged

The documentation from this folder has been merged into the
[convex-tanstack-cloudflare](https://github.com/user/convex-tanstack-cloudflare) template repository.

See the template's `docs/` folder for:
- `ARCHITECTURE.md` — patterns, gotchas, entry point patterns
- `AUTH_SOLUTION.md` — auth integration, `expectAuth` gotcha, pattern standardization
- `OPTIONAL_FEATURES.md` — feature status table, security checklist, upgrade guide

The `AI_AGENT_CONTEXT.md` has been moved to `docs/AI_AGENT_CONTEXT.md` in this repo (g-matrix-specific).

The original files are preserved in git history.
